using BLL;
using DAL;
namespace PL
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Student p1 = new Student("Anya", "Kuz", "female", 2, "AB12345678", true, "kyiv");
            //Person p2 = new Person("Bob", "MArley", "male");
            //EntityService entityService = new EntityService("db", FileExtension.JSON);
            //entityService.Add(p1);
            //entityService.Add(p2);
            //var list = entityService.GetEntities();
            Menu menu = new Menu();
        }
    }
}